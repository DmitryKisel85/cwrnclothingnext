import { Directory } from "components/common/Directory";
import Layout from "components/common/Layout";

const Home = () => {
	return (
		<Layout>
			<Directory />
		</Layout>
	);
};

export { Home };
